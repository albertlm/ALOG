Inicio
    Inteiro: numero, alg1, alg2, alg3, alg4, somatoria;
    Leia(numero);
    alg1 <- numero div 1000;
    alg2 <- (numero mod 1000) div 100;
    alg3 <- (numero mod 100) div 10;
    alg4 <- numero mod 10;
    somatoria <- alg1 + alg2 + alg3 + alg4;
    Escreva(somatoria);
Fim    